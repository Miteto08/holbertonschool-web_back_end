#!/usr/bin/env python3

"""Basic annotations - concat
mandatory"""


def concat(str1: str, str2: str) -> str:
    """function that takes a string str1 and a string str2
    as arguments and returns a concatened string."""
    return (str1 + str2)
